# Test dependencies for hw-mgmt test suite
# Install with: pip install -r requirements.txt

# REQUIRED - Core testing framework
pytest>=6.0

# Code coverage support
coverage>=5.0
pytest-cov>=2.10

# NOTE: Other pytest plugins are OPTIONAL
# The test suite is designed to work with just pytest installed
# Additional plugins can enhance functionality but are not required:
#
# - pytest-timeout: Prevent hanging tests (already commented out in pytest.ini)
# - pytest-xdist: Parallel test execution (pytest -n auto)
# - pytest-html: HTML test reports

